{
  "name": "mincemeat-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "preview": "vitepress preview .",
    "typecheck": "tsc --noEmit -p .vitepress/tsconfig.json",
    "lint:links": "node scripts/check-links.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs"
  },
  "dependencies": {
    "vitepress": "1.6.4",
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^5.6.0"
  }
}
